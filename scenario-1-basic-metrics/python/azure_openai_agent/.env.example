# Azure AI Agent Configuration
# Copy this file to .env and fill in your values
# Based on agent-framework reference: https://github.com/microsoft/agent-framework

# Authentication is handled via Azure CLI
# Run 'az login' in your terminal before running the agent

# Azure OpenAI Endpoint (required)
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/

# Deployment name (model) to use
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o-mini

# Optional: Number of iterations for performance testing (default: 1000)
# ITERATIONS=1000
